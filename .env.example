# Only needed when running the frontend and backend as two separate local
# processes (npm run dev + npm run server). Leave unset for Docker or a
# single-domain deploy, where the API is reached via a relative path.
VITE_API_URL=http://localhost:3001
